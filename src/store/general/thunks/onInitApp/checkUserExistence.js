/*
  We need this function to check if the user/linkdrop accounts, which are stored in
  the local storage, still exists. We may have a situation, when a user deleted his linkdrop
  account from another device (mobile e.g) but the current app doesn't know about this -
  it will be a cause of bugs.
 */
// TODO implement this
export const checkUserExistence = () => {};

